{
  "name": "carrinho-compras-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Testes ponta a ponta da loja, contra a stack real (nginx → API → PostgreSQL).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "typecheck": "tsc --noEmit",
    "relatorio": "playwright show-report",
    "navegadores": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "22.20.3",
    "typescript": "7.0.2"
  }
}
